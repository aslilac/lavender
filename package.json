{
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"@mckayla/prettier": "^1.0.1",
		"prettier": "^2.5.1"
	},
	"scripts": {
		"dev": "npm run dev -w lavender",
		"start": "npm run start -w lavender",
		"fmt": "prettier --ignore-path .gitignore --write . && cargo fmt",
		"lint": "npm run lint -w lavender",
		"test": "npm run test -w lavender && cargo test"
	}
}
