language: rust
rust:
  - stable
  - beta
  - nightly
cache: cargo
matrix:
  allow_failures:
    - rust: nightly

jobs:
  include:
    - 
    - language: node_js
      node_js: "12"
      script:
      - yarn ava
      
      cache: yarn